<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="AppBaseTheme" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <item name="android:colorPrimary">@android:color/holo_red_dark</item>
        <item name="android:colorPrimaryDark">@android:color/holo_red_dark</item>
        <item name="android:windowBackground">@android:color/holo_red_light</item>
        <item name="android:navigationBarColor">@android:color/holo_red_light</item>
    </style>

    <style name="TileStyle" parent="AppBaseTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">10dp</item>
        <item name="android:tint">@android:color/white</item>
    </style>

    <style name="TextStyle">
        <item name="android:textSize">@dimen/textSize</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">5dp</item>
    </style>
</resources>
